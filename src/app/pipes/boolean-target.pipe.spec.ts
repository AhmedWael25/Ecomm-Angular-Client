import { BooleanTargetPipe } from './boolean-target.pipe';

describe('BooleanTargetPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanTargetPipe();
    expect(pipe).toBeTruthy();
  });
});
